---
description: "Run a grounded web search via the agy CLI (Gemini, with citations)."
argument-hint: "<query>"
---

Perform a grounded web search for: $ARGUMENTS

If `$ARGUMENTS` is empty, ask the user what to search for before proceeding.

Follow the instructions in the `agy-web-search` skill to execute the search and return the results with citations.
